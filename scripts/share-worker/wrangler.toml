name = "overframe-share"
main = "index.js"
compatibility_date = "2024-01-01"

# KV namespace — create with: npx wrangler kv namespace create COLLECTIONS
# Then replace the id below with the output ID.
[[kv_namespaces]]
binding = "COLLECTIONS"
id = "e51642e6c8154e20946c8017533981be"

[vars]
# No extra env vars needed

# Optional: custom domain via Cloudflare dashboard → Workers → Triggers → Custom Domains
# Recommended: share.overframe.app
